package cn.edu.training.novel.domain;

/** The only product locations allowed to reference an uploaded object. */
public enum MediaAssetPurpose {
    BOOK_COVER,
    BOOK_COVER_CANDIDATE,
    HOME_CAROUSEL_BANNER
}
